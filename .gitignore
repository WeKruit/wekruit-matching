.env
!.env.example
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.mypy_cache/
.pyright/
# session artifacts (job-id dumps, secrets) — never commit
data/
.env.bak
.firebase-sa.json
# local dev artifacts — keep out of git so the nightly dirty-guard (-uno) and
# `git status` stay clean
.planning/
.worktrees/
