---
phase: 09-console-shell-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wekruit_matching/api/internal_ui.py
  - tests/test_internal_ui.py
autonomous: true
requirements:
  - CONS-01
  - CONS-02
  - A11Y-01
  - A11Y-02
---

<objective>
Create one shared console shell for Jobs, Stale, Stats, and Pipeline with a WeKruit token layer, accessible page structure, and test coverage for the shell contract.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-console-shell-design-tokens/09-CONTEXT.md
@src/wekruit_matching/api/internal_ui.py
</context>

<tasks>

1. Replace the ad hoc CSS with reusable shell, section, badge, metric-card, and pagination primitives.
2. Add page-level `h1`, summary strip, skip link, visible labels, and consistent primary navigation state.
3. Introduce token hooks for internal and external surface modes without cloning page markup.
4. Add render-level pytest coverage that exercises the shell and semantics without a live database.

</tasks>

<verify>

- `uv run ruff check src/wekruit_matching/api/internal_ui.py tests/test_internal_ui.py`
- `uv run pytest tests/test_internal_ui.py -q`
- `uv run python -m py_compile src/wekruit_matching/api/internal_ui.py tests/test_internal_ui.py`

</verify>
