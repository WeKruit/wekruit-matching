---
phase: 16-url-resolution-firecrawl-integration
plan: 01
type: execute
wave: 1
depends_on:
  - 15-01
files_modified:
  - src/wekruit_matching/config.py
  - .env.example
  - src/wekruit_matching/pipeline/firecrawl_enricher.py
  - tests/test_firecrawl_enricher.py
autonomous: true
requirements:
  - PARSE-04
  - RESOLVE-02
  - RESOLVE-03
  - RESOLVE-04
---

<objective>
Implement Workday CXS discovery plus Firecrawl scrape, extract, and search helpers with explicit asyncio timeout control.
</objective>

<tasks>

1. Add Firecrawl settings to config and environment examples.
2. Build `firecrawl_enricher.py` with timeout wrappers, Workday CXS discovery, scrape-first fallback logic, and search filtering.
3. Add focused async tests for timeout behavior, Workday discovery, scrape vs. extract escalation, and search filtering.

</tasks>

<verify>

- `uv run pytest tests/test_firecrawl_enricher.py -q`
- `uv run python -m py_compile src/wekruit_matching/pipeline/firecrawl_enricher.py tests/test_firecrawl_enricher.py`

</verify>
