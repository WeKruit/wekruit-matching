---
phase: 14-db-schema-url-classifier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0004_add_jd_fetch_tracking.py
  - src/wekruit_matching/db/tables.py
  - src/wekruit_matching/pipeline/url_classifier.py
  - tests/test_pipeline_url_classifier.py
autonomous: true
requirements:
  - PIPE2-01
  - PIPE2-02
  - RESOLVE-01
  - TEST-01
---

<objective>
Implement the fetch-tracking schema and a deterministic URL router that classifies ATS domains without network I/O.
</objective>

<tasks>

1. Add migration `0004` for `jd_fetch_source`, `jd_fetch_attempted_at`, and `ats_content_hash` plus the partial index used by the future enrichment queue.
2. Update SQLAlchemy table metadata to reflect the new tracking columns.
3. Create `url_classifier.py` with deterministic routing for Greenhouse, Lever, Ashby, Workday, and unknown domains.
4. Add unit tests for routing edge cases and query-string normalization.

</tasks>

<verify>

- `uv run pytest tests/test_pipeline_url_classifier.py -q`
- `uv run ruff check alembic/versions/0004_add_jd_fetch_tracking.py src/wekruit_matching/pipeline/url_classifier.py tests/test_pipeline_url_classifier.py`
- `uv run python -m py_compile alembic/versions/0004_add_jd_fetch_tracking.py src/wekruit_matching/pipeline/url_classifier.py tests/test_pipeline_url_classifier.py`

</verify>
